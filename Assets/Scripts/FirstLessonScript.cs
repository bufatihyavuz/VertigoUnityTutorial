using UnityEngine;

namespace LessonBasic
{
    public class FirstLessonScript : MonoBehaviour
    {
        public int tamsayi = 1;
        [SerializeField] int tamsayi2 = 100;
        [SerializeField] float ondalikSayi = 0.25f;
        float ondalikSayi2 = 5.252525f;
        bool dogruYanlis = true;

        // Start is called before the first frame update
        void Start()
        {
        }

        // Update is called once per frame
        void Update()
        {
        }
    }
}